# AGENT-HQ — ⟨Project⟩ command center

> The shared spine. **Read this first, every session.** Both Conflux elements (and the AI) record
> their load-bearing decisions here. Keep it tight — link out, don't dump. This is the one file a cold
> start can read to know *almost everything*.

## Compass (north star)
⟨One paragraph. The single thing every feature is measured against. If a change doesn't serve this,
the Visionary should question it.⟩

**We are NOT building:** ⟨explicit non-goals — the cheapest way to stay on course⟩

## Repo infra
- **Stack:** ⟨language / framework⟩  ·  **Entry:** ⟨main file⟩
- **Run:** `⟨cmd⟩`  ·  **Test:** `⟨cmd⟩`  ·  **Typecheck:** `⟨cmd⟩`
- **Key paths:** ⟨the 3–6 files that matter most⟩
- **Ports / services:** ⟨if any⟩

## The three orbs (Conflux — Body · Mind · Soul)
- **Wex · the Mind (the Loop)** — trackers live in `/root`; the full skill set:
  `conflux-start / go / verify / doctor / end / cleanup / status / visionary / council / scout / trim / bridge / vizvalaszto`;
  cross-session memory via **Grimoire**; prose compaction via **Sigil**; lean code + ask-first doc preprocess via **Trim**.
- **Quas · the Body (understanding)** — **Cartograph** (the whole repo → one graph + atoms), **Anchor**
  (cold-start freeze — memory can't drift), **Scout** (plan→gate→cite research); Radar/Crucible/Lattice ride under Cartograph.
- **Exort · the Soul (connection)** — the **Marker** (`.conflux/marker.json`, signed), the **Lens**
  (fly the graph: `node ⟨conflux⟩/exort/lens-here.mjs .`), the **Bridge** (external verdicts → proofs).

## Pipelines (recurring, repeatable)
- **Localization** *(if any)*: ⟨source → xlsx → language files; which model runs it (a cheap/fast one)⟩
- ⟨other recurring multi-step processes⟩

## Conventions (the contract)
- **Model routing:** ⟨strongest = ideation/architecture · mid = coding · cheapest = strings/mechanical — name YOUR current models here; names rot fast⟩
- **Nomenclature:** ⟨tokens not hex, i18n keys not literals, registry nodes not ad-hoc markup, …⟩
- ⟨other house rules⟩

## Load-bearing decisions (append-only, newest first)
- YYYY-MM-DD — ⟨decision⟩ — ⟨why it was made⟩
